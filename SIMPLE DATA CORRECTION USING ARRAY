#include <stdio.h>
int main() {
    int p,batch2[4];
    printf("for which student we need to correct:");
 scanf("%d",&p);
 printf("whats the actual score");
 scanf("%d",&batch2[p-1]);
    return 0;
}
